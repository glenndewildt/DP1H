using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using DP1H.Model;

namespace GateTest
{
    [TestClass]
    public class Gate
    {
        [TestMethod]
        public void AndTest()
        {
            AND n = new AND();
            n. = 1;
            n.input1 = 1;
            n.Calculate();

        }
    }
}
